import test from 'node:test'; import assert from 'node:assert/strict'; import {createFunctionSummary} from '../../../js/analysis/summary/contract.js';
function sw(p){return createFunctionSummary({functionId:'callee',returnProvenance:[p],status:{snapshotId:'s',analyzerId:'summary-test',analyzerVersion:'1',completeness:'complete',stopReason:null}});}
test('6069 structured provenance rejected',()=>{assert.throws(()=>sw({kind:'arg',argIndex:['1'],returnIndex:['0'],offset:['8']}),/function-summary-invalid-return-provenance/);assert.throws(()=>sw({kind:'arg',argIndex:true}),/function-summary-invalid-return-provenance/);assert.throws(()=>sw({kind:'arg',argIndex:0,offset:false}),/function-summary-invalid-return-provenance/);assert.throws(()=>sw({kind:'arg',argIndex:{}}),/function-summary-invalid-return-provenance/);});
test('6069 canonical indices accepted',()=>{let s=sw({kind:'arg',argIndex:1,returnIndex:0,offset:8});assert.equal(s.returnProvenance[0].argIndex,1);assert.equal(s.returnProvenance[0].returnIndex,0);assert.equal(s.returnProvenance[0].offset,'8');s=sw({kind:'arg',argIndex:2n,offset:16n});assert.equal(s.returnProvenance[0].argIndex,2);assert.equal(s.returnProvenance[0].offset,'16');s=sw({kind:'arg',argIndex:'3',offset:'0x20'});assert.equal(s.returnProvenance[0].argIndex,3);assert.equal(s.returnProvenance[0].offset,'32');});
