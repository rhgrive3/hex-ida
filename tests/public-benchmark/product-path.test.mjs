import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
test('host reuses production Backend, App range/discovery and public Query API',()=>{const src=fs.readFileSync(new URL('../../tools/validation/public-benchmark/product-host.mjs',import.meta.url),'utf8');for(const token of ['new Backend','Object.create(App.prototype)','app.ensureFunctions','app.ensureProgram','AnalysisQueryAPI','createAppAnalysisQueryAdapter'])assert.match(src,new RegExp(token.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')));assert.doesNotMatch(src,/decoded-function-adapter|decodeRiscv|createSemanticNode/)});
