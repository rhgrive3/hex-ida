// Required Semantic v2 bridge for #5763. The root regression remains the
// single assertion authority; this wrapper only connects it to required CI.
import '../issue-5763-cfg-canonical-order-locale-invariant.mjs';
