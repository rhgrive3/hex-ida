# Read-only baseline oracles

Exact, unmodified source files from Drive main archive commit
`f18c60ad3dfbd0ec16e23a9cb942fd4ac3c2c727`.
Relative layout is preserved, so no import rewriting is needed.
These files are test-only; production code must not import this directory.
`manifest.json` records archive and individual source hashes.
Do not regenerate them from the optimized implementation.
