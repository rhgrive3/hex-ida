version: 2.1

jobs:
  probe-generated-build-large:
    docker:
      - image: cimg/node:22.23
    resource_class: large
    steps:
      - checkout
      - run: npm ci
      - run: npm run userscript:build

workflows:
  diagnostic:
    jobs:
      - probe-generated-build-large
