version: 2.1

commands:
  setup-benchmark-target-check:
    parameters:
      target:
        type: string
      category:
        type: string
    steps:
      - checkout
      - attach_workspace:
          at: .
      - run:
          name: Check << parameters.target >> << parameters.category >>
          command: node scripts/ci/circleci-benchmark-target-probe.mjs << parameters.target >> << parameters.category >>

jobs:
  universal-benchmark-generate:
    docker:
      - image: cimg/node:22.23
    resource_class: medium
    environment:
      HEX_FIXTURE_BATTLECATS_URL: https://raw.githubusercontent.com/rhgrive3/hex/bd7bd61d093592d07fdd9e9b14a859e19dd4c3a9/tests/battlecats
      HEX_FIXTURE_TSUMTSUM_URL: https://raw.githubusercontent.com/rhgrive3/hex/bd7bd61d093592d07fdd9e9b14a859e19dd4c3a9/tests/TsumTsum
      HEX_FIXTURE_YWP_URL: https://raw.githubusercontent.com/rhgrive3/hex/bd7bd61d093592d07fdd9e9b14a859e19dd4c3a9/tests/YWP
    steps:
      - checkout
      - run:
          name: Fetch and verify pinned real fixtures
          command: |
            npm run fixtures:fetch
            npm run fixtures:check
      - run:
          name: Generate universal benchmark report
          command: |
            set -o pipefail
            npm --silent run binary:benchmark | tee universal-platform-benchmark.json
      - persist_to_workspace:
          root: .
          paths:
            - universal-platform-benchmark.json

  benchmark-battlecats-fixture:
    docker: [{ image: cimg/node:22.23 }]
    resource_class: small
    steps: [{ setup-benchmark-target-check: { target: battlecats, category: fixture } }]
  benchmark-battlecats-source-audit:
    docker: [{ image: cimg/node:22.23 }]
    resource_class: small
    steps: [{ setup-benchmark-target-check: { target: battlecats, category: source-audit } }]
  benchmark-battlecats-parsed-identity:
    docker: [{ image: cimg/node:22.23 }]
    resource_class: small
    steps: [{ setup-benchmark-target-check: { target: battlecats, category: parsed-identity } }]

  benchmark-tsumtsum-fixture:
    docker: [{ image: cimg/node:22.23 }]
    resource_class: small
    steps: [{ setup-benchmark-target-check: { target: TsumTsum, category: fixture } }]
  benchmark-tsumtsum-source-audit:
    docker: [{ image: cimg/node:22.23 }]
    resource_class: small
    steps: [{ setup-benchmark-target-check: { target: TsumTsum, category: source-audit } }]
  benchmark-tsumtsum-parsed-identity:
    docker: [{ image: cimg/node:22.23 }]
    resource_class: small
    steps: [{ setup-benchmark-target-check: { target: TsumTsum, category: parsed-identity } }]

  benchmark-ywp-fixture:
    docker: [{ image: cimg/node:22.23 }]
    resource_class: small
    steps: [{ setup-benchmark-target-check: { target: YWP, category: fixture } }]
  benchmark-ywp-source-audit:
    docker: [{ image: cimg/node:22.23 }]
    resource_class: small
    steps: [{ setup-benchmark-target-check: { target: YWP, category: source-audit } }]
  benchmark-ywp-parsed-identity:
    docker: [{ image: cimg/node:22.23 }]
    resource_class: small
    steps: [{ setup-benchmark-target-check: { target: YWP, category: parsed-identity } }]

workflows:
  probe:
    jobs:
      - universal-benchmark-generate
      - benchmark-battlecats-fixture: { requires: [universal-benchmark-generate] }
      - benchmark-battlecats-source-audit: { requires: [universal-benchmark-generate] }
      - benchmark-battlecats-parsed-identity: { requires: [universal-benchmark-generate] }
      - benchmark-tsumtsum-fixture: { requires: [universal-benchmark-generate] }
      - benchmark-tsumtsum-source-audit: { requires: [universal-benchmark-generate] }
      - benchmark-tsumtsum-parsed-identity: { requires: [universal-benchmark-generate] }
      - benchmark-ywp-fixture: { requires: [universal-benchmark-generate] }
      - benchmark-ywp-source-audit: { requires: [universal-benchmark-generate] }
      - benchmark-ywp-parsed-identity: { requires: [universal-benchmark-generate] }
