#!/usr/bin/env node
import fs from 'node:fs';const p=process.argv[2];if(!p){console.error('report requires summary.json');process.exit(2)}const r=JSON.parse(fs.readFileSync(p,'utf8'));const a=r.comparison?.aggregate??{};
console.log(`# ${r.suite}\n\nReference: ${r.reference?.product??'unknown'} ${r.reference?.version??''}\nScope: ${r.comparison?.scope??'unknown'}\n\nCases: ${r.total}\nStates: ${JSON.stringify(r.states)}\n\nFunction-union denominator: ${a.denominator??0}\nMatched by address: ${a.matched??0}\nIDA artifact coverage: ${((a.idaCoverage??0)*100).toFixed(2)}%\nHex pseudocode coverage: ${((a.hexCoverage??0)*100).toFixed(2)}%\n\nSemantic: UNMEASURED\nRecompilability: UNMEASURED\nCompetitor latency: UNMEASURED\n\nThis is public-artifact evidence only. It does not set nativeCompetitorsRun and consumes 0 SCPA native cells.`);
