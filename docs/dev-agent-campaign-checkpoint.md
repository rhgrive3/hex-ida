# Dev Agent self-improvement campaign — historical checkpoint index

> **Status: HISTORICAL CHECKPOINT.**  
> **Original checkpoint:** [`archive/dev-agent/dev-agent-campaign-checkpoint-pre-phase3.md`](archive/dev-agent/dev-agent-campaign-checkpoint-pre-phase3.md)  
> **Current canonical operating contract:** [`improving-agent.md`](improving-agent.md)

The archived document records a real point in the self-improvement campaign when the Versioned DOM Skill system and same-origin max-6 iframe Worker Pool had been implemented but Dynamic Task Graph work had not yet started. It must not be interpreted as the current campaign state.

Current campaign facts must be refreshed from live GitHub/runtime evidence at run start. In the current canonical contract, Phase 1 (Versioned DOM Skill System) and Phase 2 (same-origin max-6 iframe Worker Pool) are implemented, Phase 3 (Dynamic Task Graph) has been merged, and ChatGPT Project Automation is the next product campaign target. `improving-agent.md` is the authority for that roadmap.

The runtime identity table in the archived checkpoint is intentionally historical. A merged source change does not prove activation, and no committed document should hard-code an old buildId/userscript serial as present runtime truth. Use `dev.runtime.identity` whenever a decision depends on what is actually executing.

The original checkpoint is preserved unchanged at the archive path above so its historical evidence, failure analysis and resume procedure remain available.
