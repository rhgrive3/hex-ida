# Original request (user, 2026-09-23) — condensed faithfully; section numbers as given

Goal: bring rhgrive3/hex-ida to a completed state with all known important problems fixed: identify → fix → focused test → re-measure → PR/merge → final acceptance → freeze. Not investigation or design docs. FAST is the default; never move Deep/proof-heavy work back onto the default path. Speed matters a lot.

1. FAST speed: tail still heavy (test_composite_types 27 s, __addtf3 21–22 s, __multf3 16–21 s, test_array_types 19.7 s, param_thread_local_storage 18.9 s, param_varargs 13.8 s on CI). Hotspots: capturePassState, isDeepImmutable, fastJsonTextDigest, stableDigest, encode, matchesBodyCore, valueFeedsAddressOrCall, GC/allocation, work outside the pass manager (AST/Phase 8). Directions: less rescanning of immutable graphs, incremental/structural-sharing snapshots, less digest/serialization recomputation, less allocation/GC, index/worklist for repeated dataflow/use scans. Do not break rollback/proof correctness. Add before/after equivalence regressions. No function-name special cases (__addtf3 etc.); improve general CFG/dataflow patterns.
2. Past general weaknesses (reports/investigations/current-main-weakness-20260923/, OLD snapshot 09dfcb283, not current values): goto 286/2258 functions (2497 total, 75.6% in __addtf3/__multf3), unknown instructions in 95 functions, 336 unstructured, raw recompilation 32/32 cases fail, 192/192 samples fail syntax (undeclared identifier 86, undeclared local 52, unknown type 31, undeclared global 23), case-level first divergence undeclared-local 32/32. Re-measure on latest main at the end and fix only what remains.
3. Timeout semantics: `--function-timeout-ms` was not a hard timeout. Settle on either a real hard watchdog at the process/worker boundary, or documenting it clearly as best-effort in the API/CLI/docs. Do not leave something that looks hard but is not.
4. OpenTTD/C++: RTTI/vtable detection is strong (893 classes, 200/200 vtables, 354/354 typeinfos, 2305 slots / 1899 resolved / 1214 named, 16/20 complete), but the pseudocode showed 0 indirect-call markers, 0 member names and 0 class mentions. Complete the chain RTTI/class → vtable → slot target → virtual/indirect call target → receiver/class propagation → field offset → member/type → pseudocode projection, without a new giant analyzer. No invented class/member names without binary evidence. No whole-program proof that makes FAST heavy (lazy/on-demand only). Add at least one holdout with a matched debug/unstripped reference from the same build, for field/member evaluation.
5. OpenMW P0: "Analyze real game with Hex FAST" failed (~124 s, not the 600 s timeout). Add stage checkpoints once (openProduct, class count, vtable progress, snapshot, function enumeration, sample decompile, exception, memory), confirm the first failure, then production fix → focused regression → OpenMW rerun → remove diagnostics. Final condition: the OpenMW real-game job succeeds and writes result.json.
6. Pinpoint: 426-field corpus, candidate truth present 426/426. Baseline top1 282/426 (exact 229/230, partial 53/196). P4: correct strong 211, false strong 67, correct-but-ambiguous 71, wrong top1 144. The score is not a probability (score ≥ 0.99 is right in only 168/275). Keep the confidence policy (≥3 independent groups, or exact + trusted {metadata, structural} 2-group; +55 correct strong, +0 new false strong) unless there is a new counterexample. Do not fix the 144 ranking failures with threshold tuning.
7. Local binary probe: of the 61 Jev-eligible cases, 19 were already correct and 42 wrong; only 2 wrong ones were rescued; 22 were ANALYSIS_PRIMITIVE_MISSING. Fix what can be improved together with field/receiver recovery.
8. Jev has value: force-all rerank on partial 196 gave 53 → 92 (46 rescue, 7 regression, net +39, overall 282 → 321). Force-all is unsafe (7 regressions, stable across repeats, so a multiple-call consistency check is not a safe gate). The deterministic lexical comparator (152/196) has generator bias; it is not production generalization.
9. G28/#9519: G28 is post-hoc; do not treat it as validation. On the new 48 cases (36 answerable, 12 abstain): baseline 3/36, lexical 3/36, G28 routed 0/48. All 42 force-all Jev calls hit HTTP 400 "must have at most 255 choices" (Hex budget up to 400). Reject G28. The 400 errors do not measure Jev accuracy.
10. Final Jev design: keep it, never a required dependency. Hex facts → deterministic candidate lattice → deterministic ranking → Jev semantic preference only when needed → fail closed. Jev may only prefer/rerank existing candidates. It must never create addresses, facts or answers outside the candidates, and never promote a verdict by itself. API error/timeout/malformed reply → keep the local result. For the 255 limit, use a deterministic shortlist only for the Jev request (do not prune the lattice), and measure truth retention first (≈100%).
11. Settle Jev once: shortlist → ≤255 → freeze the final observable router → preference-only/fail-closed → after the freeze, a new free-form, binary-disjoint holdout → prospective evaluation. If it is safe: canonical rerank. If not: optional advisory/secondary suggestion, with the canonical Hex top1/verdict unchanged. Then end Jev development (no G29/G30/...).
12. Issue #9520: publishFixtureFile source-identity guard reused after a successful rename → phase-split the guards; regressions; close.
13. Order: A OpenMW, B FAST tail, C #9520 in parallel; then D C++ projection (OpenTTD + OpenMW + debug-reference holdout), E Pinpoint/Jev, F re-measure the general weaknesses on latest main (goto, unstructured, unknown instructions, undeclared locals, missing types/globals, raw syntax/TU recompilation), G full 160-case ARM64 benchmark, H final real-game acceptance, I final performance benchmark, J freeze.
14. Final acceptance:
    - Stability: OpenTTD and OpenMW succeed, real-game crashes 0, no known major lifecycle bug.
    - FAST: default FAST, Deep explicit, large tail improvement, proof/digest repeated-work hotspots resolved, p50/p95/p99/tail over multiple runs, timeout semantics settled.
    - Decompiler: no completeness regression, no silent unknown semantics, fewer avoidable gotos (gotos allowed for irreducible CFGs), soft-float improved.
    - C++: no RTTI/vtable regression, virtual targets in the pseudocode, binary-grounded class/member/type, field/member holdout, invented facts 0.
    - Pinpoint: recall kept, no exact regression, partial ranking improved, false strong not worse, score not treated as a probability.
    - Jev: 255 problem solved, shortlist recall verified, preference-only, fail-closed, fact minting 0, prospective holdout done, canonical/advisory decided, no router proliferation.
    - Output: locals/types/globals/TU syntax re-measured on latest main and fixed.
    - Repository: open critical/major issues 0, open fix PRs 0, focused tests green, broad acceptance green, final result/report committed, main frozen.
Prohibited: Deep back to default; proof work that slows FAST; old snapshot numbers used as current values; a full rewrite because of OpenMW; soft-float fixed by function-name hacks; Jev minting binary facts; Jev-only promotion to strong; G28 post-hoc results as validation; #9519 HTTP 400 treated as a Jev accuracy failure; threshold-only ranking fixes; repeating the same investigation; endless new research themes.
References: reports/investigations/{current-main-weakness-20260923, pinpoint-confidence-calibration, pinpoint-confidence-plan-a, pinpoint-jev-probe-audit-20260923, jev-rescue-regression-gap, jev-independent-holdout-20260923}/, Actions run 35875997864 (in repo rhgrive3/actions).
Most important: where existing evidence already explains the cause, go straight to the fix. Every step must go first failure → minimal production fix → focused regression → real measurement → merge.
