READ-ONLY AUDIT. Work in /mnt/workspace/hex-ida (do not edit or commit anything there). Write your report to /tmp/claude-0/-mnt-workspace-hex-ida/40472aea-bded-46c9-8e0c-871b3f42be35/scratchpad/audit/wall-clock-budgets.md.
Context: js/binary/dynamic-symbol-budget.js and js/binary/relocation-budget.js had DEFAULT wall-clock limits (maxWallMs 2000/1500) that silently truncated ELF dynamic symbols/relocations on slow hosts, so analysis results (RTTI class counts) changed with host speed. Those two are being fixed separately.
Task: find EVERY other place in js/ where a wall-clock time budget (Date.now()/performance.now() deadline, maxWallMs, timeBudgetMs, deadline, elapsed checks) can change analysis RESULTS (not just progress reporting) in the binary loading, symbol analysis, function discovery, program construction, RTTI/C++ discovery, and default FAST decompile paths reached from tools/validation/public-benchmark/product-host.mjs openProduct() and product.query.decompile(..., {profile:'fast'}).
For each: file:line, default value, what gets truncated/skipped, whether a warning/partial flag is surfaced to the user, whether a deterministic (operation-count) bound also exists, and whether it is an intended documented FAST valve (e.g. PassManager timeBudgetMs with deterministicTransforms) or an accidental result-changing limit. Rank by impact. Be concrete and short. Finish with the exact final line ALL-DONE.
