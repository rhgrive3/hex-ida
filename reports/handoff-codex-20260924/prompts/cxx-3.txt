IMPORTANT: Execute ALL steps to completion in this single run. Do not stop after reading files or planning; keep calling tools until the commit and summary are done. Work in small verified increments and commit intermediate progress on the branch (git commit) so work is never lost.

Work only in the git worktree /mnt/workspace/wt-cxx (branch feat/cxx-pseudocode-projection). Repo: Hex (JS decompiler, node ESM). Do not push.

NOTE: PR #9527 (already merged on this base; files js/decompiler/cxx-evidence.js, pipeline-core.js, semantic-core.js, types/high-variables.js, tests/phase8/cxx-object-decompiler-projection.test.mjs) already projects canonical C++ member types into decompiler output. Read git log -8 --stat -- js/decompiler/cxx-evidence.js and that test first; BUILD ON IT, do not duplicate it. Measure first with the realgame harness on OpenTTD on this base to see what still shows 0 in pseudocode, then fix only what remains.

PROBLEM (as measured before #9527): Hex detects C++ RTTI/vtables well on real games (OpenTTD ARM64: 893 classes, 200/200 vtables, 354/354 typeinfos, 2305 vtable slots, 1899 resolved), but FAST pseudocode does not show it: in 20 sampled decompiled functions (12 are vtable slot targets) there were 0 indirect-call markers, 0 member-access names (`->name`/`.name`), 0 class-name mentions. Internal C++ evidence exists (js/rtti.js, js/analysis/cxx/{index,rtti-evidence,virtual-dispatch,object-evidence,member-types,project}.js, reports/phase7/cxx-recovery/README.md, tests/phase7/cxx/*). The chain RTTI/class -> vtable -> slot target -> virtual/indirect call target -> receiver/class propagation -> field offset -> member/type -> pseudocode projection is not completed into the FAST decompile output.

GOAL: complete that chain into the pseudocode of the default FAST decompile (product.query.decompile(snapshot, addr, {profile:'fast'})) WITHOUT a new big analyzer, and WITHOUT whole-program proof on the FAST path (any extra work must be lazy/on-demand and bounded per function; measure that FAST latency does not regress more than ~5% on the benchmark).
Binary-grounded only: a class/member/type name may appear only when backed by binary evidence (RTTI typeinfo name, vtable membership of the function being decompiled — i.e. `this` of a function that is slot N of class C's vtable is a C* —, symbol names, DWARF when present). Never invent member names: without DWARF, members must be offset-named like `field_0x18` typed from evidence, and the receiver type `C *this` comes from vtable membership. Ambiguous (function in vtables of several unrelated classes) => use the common base if RTTI proves one, else keep untyped.

What to project (examples of acceptable output):
 - In a function that is a vtable slot of class C: signature `... C::slot_N(C *this, ...)` or a comment `// vtable slot N of C` plus `this` typed as `C *`; accesses `*(this + 0x18)` rendered as `this->field_0x18`.
 - Virtual calls: `(*(code **)(*(long *)x + 0x30))(x)` recognized as load of vptr then slot load then call; when the receiver class is known render as `x->vptr->slot_6(x)` or `C::vfunc_6(x)` / the resolved slot target name `C::method` when the slot target has a symbol; when unknown still render the indirect call explicitly (e.g. `(*(code **)(*x + 0x30))(x)`) — never silently drop it.
Look at how the realgame harness counts markers (regexes) in /tmp/claude-0/-mnt-workspace-hex-ida/40472aea-bded-46c9-8e0c-871b3f42be35/scratchpad/actions/scripts/hex-realgame-holdout.mjs: indirect call markers /\(\*\*|\(\*[^\n]{0,80}\)\s*\(/g, member names /(?:->|\.)\s*([A-Za-z_][A-Za-z0-9_]*)/g, class mention = class name substring. Your output must make these honest (do not game regexes with fake text).

Real binaries for validation (read-only):
 - OpenTTD ARM64 stripped: /tmp/claude-0/-mnt-workspace-hex-ida/40472aea-bded-46c9-8e0c-871b3f42be35/scratchpad/openttd/root/usr/games/openttd
 - OpenMW ARM64 stripped: /tmp/claude-0/-mnt-workspace-hex-ida/40472aea-bded-46c9-8e0c-871b3f42be35/scratchpad/openmw/root/usr/games/openmw (note: on current main this fails to open due to a bug being fixed on another branch; cherry-pick is not allowed — use OpenTTD for development).
 Run the realgame harness locally: node /tmp/claude-0/-mnt-workspace-hex-ida/40472aea-bded-46c9-8e0c-871b3f42be35/scratchpad/actions/scripts/hex-realgame-holdout.mjs --target-root . --binary <bin> --game openttd --target-sha local --out /tmp/claude-0/-mnt-workspace-hex-ida/40472aea-bded-46c9-8e0c-871b3f42be35/scratchpad/cxx-openttd-result.json (takes a few minutes). Record before/after totals (indirect call markers, member names, class mentions, completeness, per-function elapsed).

Debug-reference holdout (required): obtain at least one C++ ARM64 binary WITH matching debug info from the same build: e.g. an Ubuntu noble arm64 C++ package with virtual classes and its -dbgsym .ddeb from http://ddebs.ubuntu.com/pool/... (arm64 ddebs exist; verify build-id match via readelf -n on both), or build one yourself with `clang++ --target=aarch64-linux-gnu` + `ld.lld` (no sysroot available: use -nostdlib, -fno-exceptions, provide minimal stubs for __cxa_pure_virtual and the __cxxabiv1 typeinfo vtables so RTTI is emitted) keeping both an unstripped (-g) and stripped copy. Use DWARF (readelf --debug-dump=info or llvm-dwarfdump) ONLY as the scoring oracle: for each field access Hex projects in the stripped binary, check class + offset against DWARF members (DW_TAG_member data_member_location). Report precision (projected member offsets that exist in DWARF for that class) and invented facts (must be 0). Store the holdout + scorer + results in reports/investigations/cxx-projection-holdout-20260924/ (small files only, no big binaries; record URLs+sha256 of binaries).

Tests: add focused tests in tests/phase7/cxx/ for: vtable-member function gets typed this; offset member access projection; virtual call rendering with known and unknown receiver; ambiguity => no class claimed; no name without evidence. Run node --test tests/phase7/cxx/*.test.mjs and node tests/decompiler/run.mjs (tail output) and ensure green.
Also run a FAST latency sanity check on 3 benchmark binaries before/after with: node /tmp/claude-0/-mnt-workspace-hex-ida/40472aea-bded-46c9-8e0c-871b3f42be35/scratchpad/dec.mjs . benchmarks/public/codefuse-arm64/inputs/<file>.bin first:40 <out.json>  (prints per-function ms and output sha).
Commit: "feat(cxx): project binary-grounded class/vtable/member facts into FAST pseudocode" + blank line + "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>". Print a summary with before/after numbers.

ADDITIONAL FINDING (from the real-game CI run on OpenMW/OpenTTD): the harness samples "vtable slot targets" via js/rtti.js readVtable(read, vtable, symbols, 32, ctx), which reads a FIXED number of slots with no end-of-vtable detection. 10/20 OpenMW and 4/20 OpenTTD sampled "virtual targets" were not code (values 2, 0x200000000, 0x100000000, typeinfo/typeinfo-name addresses of the NEXT vtable) and decompiled as 'unsupported'. Production already has a structural sub-table boundary (see commit dff2dce3b "fix(cxx): find the vtable sub-table boundary structurally", js/analysis/cxx/). Make readVtable (or a new exported helper used by consumers) stop at the structural vtable end: a slot is a virtual function slot only if it resolves into an executable mapped region (or is a known pure-virtual/import symbol); stop at the first slot that is 0-offset-to-top/typeinfo pattern of the next vtable or non-executable data. Keep `slotCount` exact mode behaviour. Add tests. Then update /tmp/claude-0/-mnt-workspace-hex-ida/40472aea-bded-46c9-8e0c-871b3f42be35/scratchpad/actions/scripts/hex-realgame-holdout.mjs ONLY IF it needs a change to use the new helper (do not commit in that repo; just tell me the diff).
