CRITICAL: Do NOT end your turn after writing a todo list or plan. After any todo update immediately continue with the next tool call. The run is finished only when a git commit exists and the listed tests pass.

Work only in the git worktree /mnt/workspace/wt-red2 (branch fix/root-red-auth-graph). Repo: Hex (node ESM). Do not push.
PROBLEM: on current main the root issue regression gate (npm run root-issue-regressions:test, which runs every tests/issue-* file) is RED. Your files: ADDITIONAL SCOPE: the auth suite (npm run auth:test = node --test tests/auth/*.test.mjs, plus tests/auth/build-acceptance.mjs) is also red on main: "privileged graph kind discriminator fails closed", "JSONC loader accepts inline comments...", "canonical production deploy validates the default config before invoking Wrangler", and three "issue #9273" filesystem-root graph tests. A previous run already committed fixes for the 6 root files on this branch (HEAD 8fa64180a) - keep them. Fix the auth suite the same way.
tests/issue-9217-auth-config-duplicate-binding.test.mjs tests/issue-9296-auth-entrypoint.test.mjs tests/issue-9310-auth-build-policy-windows-case.test.mjs tests/issue-9230-sync-generated-userscript-names.test.mjs tests/issue-9320-standard-graph-windows-case.test.mjs tests/issue-9396-privileged-graph-provenance.test.mjs
For EACH file: run it alone (node --test <file> for *.test.mjs, node <file> for plain .mjs), read the failure, find the FIRST divergence, and fix the real cause. Decide per failure whether (a) production code regressed (fix production, minimal change), (b) the test fixture is stale versus a later intended contract change (update fixture while preserving the security/correctness property the test protects; do not weaken assertions), or (c) the test is not hermetic (depends on host env vars such as JEV_PRUNING_HOME, HOME, PATH, platform, network, or on other tests) — make it hermetic. Use git log -S / git log -- <file> to find the commit that broke it. Never delete or skip a test, never add it to exclusions.
Finally run all your files again one by one AND node --test tests/auth/*.test.mjs AND node tests/auth/build-acceptance.mjs (if it needs a browser/network, say so); all must pass. Also run: node tests/root-issue-regressions.mjs is too slow — do not run it.
Commit (new commit on top): "fix: restore auth suite regressions" + blank line + short explanation of each root cause + blank line + "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>". Print a per-file summary: root cause class (a/b/c) and fix.
