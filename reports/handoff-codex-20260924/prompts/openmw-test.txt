Work only in the git worktree /mnt/workspace/wt-openmw (branch fix/openmw-elf-plt-resolver-bounded). Do not push. An uncommitted production change already exists in js/binary/elf-dynamic-original.js (see `git diff`): findAarch64StructuralPltResolver now scans executable PT_LOAD bytes exhaustively only when the executable file bytes are <= 1 MiB (AAELF64_PLT_RESOLVER_SCAN_LIMIT_BYTES); above that it verifies only the GOT-anchored candidate (every R_AARCH64_JUMP_SLOT GOT slot's file-backed initial 8-byte value must be the same address A; A must pass the full resolver+thunk structural proof). Reason: OpenMW ARM64 (23.7 MB, 13 MB .text) failed to open because the scan pulled all of .text into the 16 MiB source-backed metadata budget ("binary metadata exceeds the 16777216-byte cache limit"), so the whole binary opened as "Raw binary" and the real-game job failed with ARM64_SLICE_UNAVAILABLE.

TASK: add regression tests; do not change the production logic unless a test proves a bug (then explain).
Add tests/phase4/binary/function-discovery-plt-resolver-large-image.test.mjs, reusing the fixture-building approach of tests/phase4/binary/function-discovery-plt-resolver.test.mjs (copy helpers as needed) but with an executable PT_LOAD whose file size is > 1 MiB (pad with NOP 0xd503201f or zeros; keep the file size modest, e.g. 1.2 MiB). Cases:
 1. GOT slots initialised with the resolver address -> exactly one 'elf-plt-structure' seed at the resolver, metadata.aarch64PltResolver.candidate === 'got-initial-value'.
 2. GOT slots disagree (one slot has another value) -> no seed.
 3. GOT slots all zero -> no seed (and no exception).
 4. GOT points to an address whose structure is broken (e.g. wrong thunk order) -> no seed.
 5. A second structurally complete resolver copy elsewhere in the big segment does not create a second seed and does not change the anchored result.
 6. Source-backed parse: using parseSourceRanges from js/binary/source-reader.js with parseELF from js/binary/index.js and a small maxCachedBytes (e.g. 512 KiB, larger than the metadata but smaller than the executable segment) succeeds and yields the same seed — this proves the parser no longer reads the whole executable segment. Build the ByteSource object like: {size:BigInt(bytes.length), maxReadLength: 1<<24, read: async(o,l)=>bytes.subarray(Number(o),Number(o)+l), readExactly: async(o,l)=>bytes.slice(Number(o),Number(o)+l)}.
 7. Small image (< 1 MiB) behaviour unchanged: existing tests still pass.
Run: node --test tests/phase4/binary/function-discovery-plt-resolver*.test.mjs  and also node tests/universal-binary-source.mjs and node tests/binary-platform.mjs.
Also verify case 6 FAILS on the old code (git stash the js change, run, git stash pop).
Then commit BOTH the production change and the test: message "fix(elf): bound PLT0 resolver discovery so large ARM64 images open" + blank line + "Source-backed parsing scanned every executable byte to prove PLT0 uniqueness, exceeding the 16 MiB metadata budget on OpenMW ARM64 so the file opened as Raw binary (ARM64_SLICE_UNAVAILABLE). Above 1 MiB of executable bytes the resolver is now anchored by the lazy GOT initial values and fully verified; disagreement fails closed." + blank line + "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>". Print a summary.
