Work only in the git worktree /mnt/workspace/wt-idmemo (branch perf/identity-text-memo). Repo: Hex, a JS decompiler (node, ESM). Do not push.

GOAL: speed up js/decompiler/phase8/analysis-identity.js WITHOUT changing any output digest/identity value (bit-for-bit identical results).

Measured problem (function __addtf3): canonicalAnalysisIdentity() takes ~13 s. Inside, typedIdentityText(shape) builds a 216,612,163-char string; `encode` is invoked 841,462 times for only 101,599 distinct objects (50,453 objects are encoded repeatedly, some thousands of times, e.g. empty objects / {start,end} ranges / shared definition shapes from definitionCache). ssaIdentityDigest builds another 75M-char string (293,910 encodes of 72,987 distinct objects). fastJsonTextDigest then hashes the flat text.

Required change:
1. In typedIdentityText(root), add a per-call Map memo (object -> encoded text) for ALL objects (not only deeply frozen). Within one synchronous typedIdentityText call nothing mutates, so an object's encoding is a pure function of the object. Keep cycle detection semantics exactly: check `active.has(value)` BEFORE consulting the memo only if that preserves the thrown errors; an object in `active` is never in memo (memo set after completion), so consult memo first is fine only if a completed object cannot be on the active stack — verify reasoning and keep the TypeError('identity-cyclic-semantic-metadata') behaviour identical. Keep the existing FROZEN_IDENTITY_TEXT cross-call cache for deeply frozen objects unchanged.
2. Avoid materialising one giant flat string where possible: replace `fastJsonTextDigest(typedIdentityText(value))` in fastJsonGraphDigest with a streaming hasher that consumes the same character sequence in pieces (4 lanes, identical math to fastJsonTextDigest: for each UTF-16 code unit c: h = Math.imul(h ^ c, P) >>> 0, same seeds/primes, same final hex formatting). The encoder must feed pieces in exactly the order the old string concatenation produced. Memoized sub-texts can be fed repeatedly (re-hashing repeated chars is unavoidable and fine). Note Map/Set encodings sort entry texts: those need the full child texts (use memoized strings); objects/arrays are emitted in order. If streaming makes the code too complex, do step 1 only and keep fastJsonTextDigest over the resulting string (still exact).
3. Keep fastJsonTextDigest exported/behaving the same for other callers.

Equivalence proof (mandatory):
- tests/performance/analysis-identity.test.mjs compares against tests/helpers/analysis-identity-baseline-oracle.mjs (the original implementation). It must pass: node --test tests/performance/analysis-identity.test.mjs
- Add tests/performance/analysis-identity-memo-equivalence.test.mjs: build IRs (use tests/phase8/helpers/ir-fixtures.mjs `fixture`) with heavy object sharing (same origin object on many values/instructions, shared {start,end} objects, shared metadata objects, Map/Set metadata, sparse arrays, -0, bigint, unicode strings, a shared object appearing at different depths) and assert canonicalAnalysisIdentity(current) deepEqual the oracle's for each, plus cyclic metadata still returns the same invalid result as the oracle.
- Also run: node --test tests/phase8/scalar/sccp.test.mjs tests/phase8/provenance/validation.test.mjs tests/phase8/scalar/c2-02-adversarial-matrix.test.mjs tests/phase7/issues-5295-5470-5475-5480-phase8-truncation.test.mjs tests/semantic-v2/c4-return-control-target.test.mjs tests/phase8/structuring/loop-control-projection.test.mjs
- Real-binary check: run this before and after your change and confirm identical `sha` fields and report elapsed ms:
  node /tmp/claude-0/-mnt-workspace-hex-ida/40472aea-bded-46c9-8e0c-871b3f42be35/scratchpad/dec.mjs . benchmarks/public/codefuse-arm64/inputs/c50d30201c285eb4343ee8adce1a67e527a7692dc0924be1e339fe4e4c672709.bin 5872
  (for "before", use `git stash` of your js change, then `git stash pop`). The script prints JSON with ms and sha. It takes ~35 s per run.

When all green, git add only changed/added files and commit: "perf(phase8): memoize typed identity encoding per call" + blank line + "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>". Print a final summary: before/after ms, whether sha identical, test results.
