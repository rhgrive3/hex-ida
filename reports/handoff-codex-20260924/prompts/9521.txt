IMPORTANT: Execute ALL steps to completion in this single run; do not stop after planning.
Work only in the git worktree /mnt/workspace/wt-9521 (branch fix/9521-deploy-snapshot-nofollow). Do not push.
Fix GitHub issue #9521 in scripts/deploy-production.mjs (read it: gh issue view 9521 -R rhgrive3/hex-ida). Problem: local copyTree() (~line 270) classifies children from readdirSync(withFileTypes) Dirents and then re-opens srcChild by pathname with readFileSync (follows symlinks) and recurses by pathname, so a child replaced by a symlink/different inode after enumeration is copied into the production asset snapshot.
Fix: for each child, lstat it and require it matches the Dirent kind; open files with fs.openSync(path, O_RDONLY|O_NOFOLLOW); fstat the fd and require isFile() and (dev,ino) equal to the lstat identity; read the bytes from the fd (readSync loop until EOF or size), then fstat again and fail closed if size/mtimeMs/ino changed; close in finally. Directories: open with O_RDONLY|O_DIRECTORY|O_NOFOLLOW, fstat, compare (dev,ino) with lstat, then recurse; also re-verify after recursion. Pre-existing symlink entries stay skipped as today. On mismatch throw Error with code 'DEPLOY_ASSET_CHILD_CHANGED'. Add small injectable seams (e.g. an optional fs-ops object parameter used only by tests) following the style of tests/issue-9479-deploy-dist-asset-snapshot.test.mjs.
Add tests/issue-9521-deploy-snapshot-child-identity.test.mjs (node:test): regular file replaced by absolute external symlink between enumeration and read -> fails closed, EXTERNAL bytes never in snapshot; relative escaping symlink -> fails closed; replaced by different regular inode -> fails closed; nested directory swapped for symlink -> fails closed; unchanged tree snapshots normally and byte-identical.
Run: node --test tests/issue-9521-deploy-snapshot-child-identity.test.mjs tests/issue-9*deploy*.test.mjs  — all must pass. Confirm the new test fails on old code (git stash scripts/deploy-production.mjs; run; git stash pop).
Commit only the script and new test: "fix(deploy): bind snapshot copies to no-follow opened child identity (#9521)" + blank line + "Closes #9521" + blank line + "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>". Print a short summary.
