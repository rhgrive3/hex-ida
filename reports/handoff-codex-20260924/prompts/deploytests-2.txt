CRITICAL: Do NOT end your turn after writing a todo list. Never reply with only a plan. After any todo update, immediately continue with the next tool call. Your run is only finished when the git commit exists and tests pass.

IMPORTANT: Execute ALL steps to completion in this single run; do not stop after planning.
Work only in the git worktree /mnt/workspace/wt-9521 (branch fix/9521-deploy-snapshot-nofollow). Do not push.
Problem: on current main these existing tests fail (they pass individually only for some): tests/issue-9313-deploy-config-snapshot.test.mjs (3 fail), issue-9319-deploy-snapshot-create-cleanup (2), issue-9341-deploy-cleanup-result (4), issue-9356-deploy-stable-fd-handoff (3), issue-9405-deploy-snapshot-cleanup (1). Typical error: "Production assets directory must be ./dist." — later production hardening (#9438 asset root admission, #9479 dist asset snapshot, #9504 entrypoint checks) made runProductionDeploy in scripts/deploy-production.mjs require a wrangler config with assets.directory ./dist, a real dist directory and regular worker-entry.js; these older tests' fixtures predate that.
Task: update ONLY the old tests' fixtures/setup (not production semantics) so each test again exercises its ORIGINAL intent (read each test's name/comments) against the current production contract — e.g. write assets.directory "./dist" in the fixture wrangler config, create dist/index.html, a regular worker-entry.js, and extend any injected fake fs seams (openSync/fstatSync/lstatSync/readdirSync/readSync) so the new dist snapshot copy path works. Do not weaken assertions. If a test's original assertion is truly impossible under the current contract, explain precisely and adapt minimally while keeping the security property it protected.
Run each file individually and all together: for f in tests/issue-9*deploy*.test.mjs; do node --test $f; done ; all must pass (34 tests total incl. issue-9521).
Commit: "test(deploy): update pre-#9479 deploy fixtures to the ./dist asset contract" + blank line + "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>". Print a summary.
