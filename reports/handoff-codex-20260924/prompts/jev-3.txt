POINTERS (use these; do not spend time rediscovering): Pinpoint production code is js/pinpoint.js, js/pinpoint-legacy.js, js/pinpoint-jev-eligibility.js. The old 426-field corpus replay/eval harness with Jev calls is reports/investigations/jev-rescue-regression-gap/{replay.mjs,evaluate.mjs,analyze.mjs,validate.mjs} (read its README "Reproduce" section) and the #9519 holdout harness is reports/investigations/jev-independent-holdout-20260923/{collect.mjs,score.mjs,holdout-cases.json}. Real binaries: tests/.real-fixtures (battlecats), tests/TsumTsum, tests/YWP; fetch others with scripts/fetch-real-fixtures.mjs (see tests/fixtures/large-fixtures.json). For the new binary-disjoint holdout prefer Mach-O apps with ObjC metadata available via that fetcher; if none exist beyond the three, say so explicitly and use binary-disjoint ELF C++ binaries with RTTI (e.g. /tmp/claude-0/-mnt-workspace-hex-ida/40472aea-bded-46c9-8e0c-871b3f42be35/scratchpad/openttd/root/usr/games/openttd) only if Pinpoint supports them.
Commit after each step (shortlist+retention, router freeze, holdout lock, evaluation, decision) so progress is never lost.

Work only in the git worktree /mnt/workspace/wt-jev (branch feat/jev-final-decision). Repo: Hex (binary analysis, node ESM). Do not push. OPENJEV_API_KEY is set in the environment (endpoint https://api.openjev.sh/v1/systemone, see reports/investigations/jev-independent-holdout-20260923/collect.mjs for the request format). Never print the key.

CONTEXT (read first, do not redo old studies): reports/investigations/jev-rescue-regression-gap/README.md, reports/investigations/jev-independent-holdout-20260923/production-routing-evidence.md and its collect.mjs/score.mjs/holdout-cases.json, reports/investigations/pinpoint-confidence-plan-a/README.md, reports/investigations/pinpoint-jev-probe-audit-20260923/.
Facts: Pinpoint (Hex's field/member locator) produces a candidate list (budget up to 400). Old 426-field corpus: candidate truth present 426/426; baseline top1 282/426 (exact 229/230, partial 53/196). Force-all Jev rerank on partial 196: 53 -> 92 (46 rescue, 7 regression). G28 router is REJECTED (post-hoc). In #9519 all 42 Jev calls failed with HTTP 400 "Too many choices: must have at most 255 choices" because Hex sent up to 400 candidates, so Jev accuracy was never measured there.

GOAL: settle Jev once, with ONE frozen design. No G29/G30 router generations.
Rules: Jev may only express a preference among EXISTING Hex candidates. It must never create addresses, field/type/RTTI facts, answers outside the candidate list, or promote a verdict to strong by itself. Any API error/timeout/malformed reply => keep Hex's local result unchanged (fail closed). Jev must stay optional (no hard dependency).

STEPS
1. Deterministic shortlist: implement a pure function `jevShortlist(candidates, {max:255})` (put it in js/pinpoint/ or wherever Pinpoint ranking lives — find it with git grep for the Pinpoint candidate/ranking code used by the old corpus harness) that keeps Hex's deterministic top-K by the existing Hex score order (stable tiebreak by candidate id/address), K<=255 (choose K=64 unless retention data says otherwise). The full candidate lattice is NOT pruned; only the Jev request uses the shortlist.
2. Measure truth retention of the shortlist BEFORE any Jev call on (a) the old 426 corpus and (b) the #9519 48-case holdout: fraction of cases whose gold is inside the shortlist. Require >= 99% on answerable cases; if not, raise K (<=255) and re-measure. Record in reports/investigations/jev-final-decision-20260924/shortlist-retention.json.
3. Freeze ONE router before seeing any new holdout: simplest observable rule, e.g. "call Jev only for partial-mode queries when Hex top1 is not strong (per existing confidence policy); take Jev's choice only if it is inside the shortlist; Jev's choice can change top1 but the verdict strength stays at most 'likely/ambiguous' (never strong from Jev alone)". Write it to reports/investigations/jev-final-decision-20260924/router-freeze.json with a sha256 of the router source file and a UTC timestamp. Also implement it in production code as an OPTIONAL advisory hook: `rerankWithJev(query, hexResult, {client, enabled})` that returns {top1, source:'hex'|'jev', advisory:{jevChoice, withinShortlist}} and returns the Hex result unchanged on any error. Default disabled.
4. AFTER the freeze, build a NEW prospective holdout: free-form natural-language partial queries on binaries NOT used before (not battlecats, TsumTsum, YWP). Find other Mach-O/ELF binaries with real metadata ground truth in the repo (tests/.real-fixtures, tests/fixtures, benchmarks, large-fixtures.json via scripts/fetch-real-fixtures.mjs if downloadable). Gold targets must be verified to exist in binary metadata. At least 40 answerable + 10 abstain-gold cases. Write queries WITHOUT looking at Hex's candidate ranking output. Hash-lock the case file in a manifest before running anything.
5. Run prospective evaluation: arms A=Hex baseline, B=Hex+frozen router (Jev). Collect raw responses (jsonl), HTTP errors, latency. Score: answerable top1, rescues, regressions, false strong, unsafe confident, API error count. Verify no HTTP 400 choice-limit errors occur.
6. DECISION (write reports/investigations/jev-final-decision-20260924/README.md with the numbers): if B has rescues > 0, regressions <= 1 AND regressions/rescues <= 0.1, zero new false strong, and fail-closed verified => "canonical rerank" (enable the hook by default only for the routed class). Otherwise => "optional advisory": keep default disabled, surface jevChoice only as advisory/secondary suggestion, canonical Hex top1/verdict unchanged. State clearly that Jev development ends here.
7. Tests: add tests/pinpoint-jev-shortlist.test.mjs covering: shortlist size <=255 for 400 candidates, determinism, retains Hex top1, fail-closed on thrown client / timeout / malformed / out-of-shortlist choice, Jev never raises strength to strong, no fact minting (returned top1 is always one of the input candidates). Run it and existing pinpoint tests (git grep -l pinpoint tests | head -30).
Commit everything (code, tests, report JSON/MD; no API keys): "feat(pinpoint): final Jev decision with deterministic <=255 shortlist" + blank line + "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>". Print a summary with shortlist retention, holdout numbers and the decision.
