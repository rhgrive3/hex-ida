Work only in the git clone /tmp/claude-0/-mnt-workspace-hex-ida/40472aea-bded-46c9-8e0c-871b3f42be35/scratchpad/actions (GitHub repo rhgrive3/actions, default branch main). First run `git pull --ff-only`.

TASK: create a manually-dispatchable real-game workflow and add stage checkpoints to the real-game script.
1. Create .github/workflows/hex-realgame-dispatch.yml by copying ONLY the `realgame` job (and the `realgame-aggregate` job) from .github/workflows/hex-realgame-tail-parallel.yml. Trigger: `workflow_dispatch` with inputs `target_sha` (required string), `target_repo` (default rhgrive3/hex-ida), `games` (default "openmw,openttd"). Use inputs for the checkout repository/ref and filter the matrix by games (use a small first job that emits the matrix JSON from the input, then `fromJSON`). Keep ubuntu-24.04-arm, the package acquisition, the RTTI oracle step, and the analysis step. Set the analysis step `timeout 900s` and job timeout-minutes 30. Always upload evidence (if: always()).
2. Edit scripts/hex-realgame-holdout.mjs: add a stage checkpoint writer. After each stage write/overwrite `<dir of --out>/stages.json` atomically (write tmp then rename) with {schema:'hex-realgame-stages/v1', stages:[{stage, ok, ms, at, detail}], lastStage, memory:process.memoryUsage().rss}. Stages: openProduct (include product.profile and product.unsupported/reason), findCxxClasses (class count), vtables (progress every 50 classes: done/total), snapshot, functions (count), selectTargets (count), decompile (progress per target: address, ms, completeness, error), result. Wrap the main body in try/catch so any exception records {stage:'error', message, stack(first 10 lines), lastStage} to stages.json before rethrowing. If product.unsupported, record it in stages.json before throwing. Do not change result.json format.
3. Commit with message "realgame: dispatchable workflow + stage checkpoints" and `git push origin main`.
4. Print the workflow file path and a summary.
