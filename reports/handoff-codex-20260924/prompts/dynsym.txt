Work only in the git worktree /mnt/workspace/wt-dynsym (branch fix/deterministic-dynsym-budget). Do not push. Uncommitted production changes already exist (see git diff): (1) js/binary/dynamic-symbol-budget.js and js/binary/relocation-budget.js: default maxWallMs is now Infinity (a wall-clock stop applies only when a caller passes an explicit maxWallMs), snapshots report maxWallMs null when infinite. Reason: on a loaded host the 2000/1500 ms defaults silently truncated OpenMW ARM64 dynamic symbols (38388 -> ~12000) and relocations, so RTTI class/vtable counts changed with host speed. (2) js/binary/elf-dynamic-original.js boundedStringAt: DT_NEEDED/DT_SONAME scan budget now charges bytes up to the NUL (name.length+1) instead of the whole 1 MiB window per name; before, ~8 DT_NEEDED names exhausted the 8 MiB scan budget (OpenMW has 39).
Do NOT change production logic. Add regression tests:
 A. tests/phase4/binary/deterministic-dynamic-budgets.test.mjs: with an injected `now` that jumps +10 minutes per call, createDynamicSymbolBudget() and createRelocationBudget() without maxWallMs never stop for wall-clock reasons (exercise checkWall / the operation path many times); with explicit maxWallMs:5 they still stop with the wall-clock reason; snapshot().maxWallMs is null by default and 5 when explicit. Also: parsing the same synthetic ELF (build one with e.g. 3000 dynamic symbols, reuse helpers from tests/issue-513-elf-symbol-budget.mjs) with a slow injected clock is impossible via public API, so instead assert parseELF output symbol count equals the dynsym count and metadata has no 'budget exceeded' diagnostic.
 B. tests/phase6/elf/dt-needed-scan-budget-actual-length.test.mjs: build an AArch64 ELF (reuse builders from tests/phase6/elf/issue-8688-dt-needed-aggregate-string-budget.test.mjs) with a DT_STRTAB larger than 1 MiB and 40 DT_NEEDED short names; assert all 40 library names are decoded and no 'dynamic strings:resource-budget' diagnostic; keep the existing #8688 adversarial tests passing (long distinct suffixes still hit the budget).
 Verify B fails on old code: git stash push js/binary/elf-dynamic-original.js; run; git stash pop. Verify A fails on old code similarly for the budget files.
Run: node --test tests/phase4/binary/deterministic-dynamic-budgets.test.mjs tests/phase6/elf/dt-needed-scan-budget-actual-length.test.mjs tests/phase6/elf/issue-8688-*.test.mjs tests/phase4/binary/issue-3692-dynamic-symbol-budget-typed-limits.test.mjs tests/phase4/binary/issue-5136-relocation-budget-typed-limits.test.mjs ; node tests/issue-513-elf-symbol-budget.mjs ; node tests/issue-495-elf-relocation-budget.mjs ; node tests/binary-budget-validation.mjs
Commit production + tests: "fix(elf): make dynamic symbol/relocation budgets deterministic; charge DT_NEEDED scans by length" + blank line + one paragraph explaining the OpenMW evidence above + blank line + "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>".
