Work only in the git worktree /mnt/workspace/wt-timeout (branch fix/function-timeout-semantics). Repo: Hex (node ESM). Do not push.

PROBLEM: `--function-timeout-ms` looks like a hard per-function timeout but is not. It only aborts an AbortController signal (best-effort). Measured: with 10000 ms a function ran 149 s and PASSED; with 30000 ms functions ran 31+ s and PASSED. Occurrences:
- reports/investigations/current-main-weakness-20260923/harness/measure-functions.mjs (parent, spawns case workers) and case-worker.mjs (line ~134 setTimeout -> controller.abort).
- tools/validation/public-benchmark/fresh-subject.mjs, profile-fresh.mjs, run-fresh-case.mjs, run-fresh.mjs.

REQUIRED OUTCOME (pick this design, do not ask): make it a REAL hard watchdog at the process boundary, and make naming/docs honest.
1. The per-function decompile in the child must report progress to the parent (e.g. a line on stdout/IPC like {type:'function-start', address, startedAt} and 'function-end'). The parent runs a watchdog: if a function has been running longer than functionTimeoutMs + a small grace (default 2000 ms, option --function-timeout-grace-ms), the parent SIGKILLs the child, records that function with state 'TIMEOUT' (hard: true, elapsedMs = the watchdog elapsed), and respawns the child to continue with the remaining functions of that case (skip already-completed and the timed-out function). The case result must remain complete and machine-readable. Keep the existing in-process AbortController as the first (soft) stage; its abort result must be recorded as 'TIMEOUT' too (not PASS) if elapsed exceeded the limit — i.e. a function whose measured elapsed > functionTimeoutMs must never be reported PASS.
2. Apply the same rule to the tools/validation/public-benchmark fresh runners: read the code, find where the per-function timeout is applied; if they run in a child process/worker, add the same hard watchdog; if not feasible there without large rewrite, then (a) rename the option in help/doc text to "best-effort abort" AND (b) mark any function whose elapsed exceeds the limit as timeout (never PASS). Keep existing CLI flag names working.
3. Document the final semantics in a short section in docs (create docs/FUNCTION_TIMEOUT_SEMANTICS.md) and in the harness header comments.
4. Tests: add tests/issue-function-timeout-hard-watchdog.test.mjs (node:test) that exercises the parent watchdog logic with a fake child script that hangs in a busy loop for one function (while(true){}) and completes others; assert: the hung function is TIMEOUT with hard:true within timeout+grace+slack, other functions complete, total run finishes. Also a unit test that elapsed>limit is never PASS. Keep tests fast (<10 s) by using small timeouts (e.g. 300 ms) and fake children instead of the real decompiler.
5. Run the new tests plus any existing tests that reference these files (git grep for their names in tests/) and make them pass.

Commit only your changes: "fix(bench): hard per-function watchdog; timeout is never PASS" + blank line + "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>". Print a summary of what changed and test results.
