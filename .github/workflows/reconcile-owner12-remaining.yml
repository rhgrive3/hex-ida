name: Reconcile owner12 remaining fixes

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  dormant:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo dormant
