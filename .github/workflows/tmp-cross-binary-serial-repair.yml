name: Temporary cross-binary serial repair

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/tmp-cross-binary-serial-repair.yml'

concurrency:
  group: tmp-cross-binary-serial-repair
  cancel-in-progress: true

jobs:
  superseded:
    runs-on: macos-latest
    steps:
      - run: echo 'Held while current main parallel-4096 cross-binary repair is proven; no repository writes.'
