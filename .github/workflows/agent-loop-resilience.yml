name: Agent loop resilience

on:
  pull_request:
    paths:
      - 'js/userscript/dev/**'
      # Direct production dependencies of the resilience suite: the
      # coordinator imports the worker contracts and chatgpt-adapter outside
      # js/userscript/dev, so changes to them must run the regressions (#5904).
      - 'js/ai/dev/workers/contracts.js'
      - 'js/userscript/chatgpt-adapter.js'
      - 'tests/dev-agent/agent-loop-resilience.mjs'
      - '.github/workflows/agent-loop-resilience.yml'
  push:
    branches: [main]
    paths:
      - 'js/userscript/dev/**'
      - 'js/ai/dev/workers/contracts.js'
      - 'js/userscript/chatgpt-adapter.js'
      - 'tests/dev-agent/agent-loop-resilience.mjs'
      - '.github/workflows/agent-loop-resilience.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: agent-loop-resilience-${{ github.event.pull_request.number || github.run_id }}
  cancel-in-progress: true

jobs:
  resilience:
    if: ${{ github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Run unattended agent loop regressions
        run: node tests/dev-agent/agent-loop-resilience.mjs
