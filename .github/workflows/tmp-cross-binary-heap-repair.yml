name: Temporary cross-binary heap repair

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/tmp-cross-binary-heap-repair.yml'

concurrency:
  group: tmp-cross-binary-heap-repair
  cancel-in-progress: true

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Superseded by serialized cross-binary OOM repair; no repository writes.'
